syntax = "proto3";
package proto;
option go_package = "/pb";

message Request {
  string input = 1;
}

message Reply {
  string output = 1;
}
//protoc -I. --go_out=plugins=grpc:. hello.proto
service Hello{
  rpc HelloWorld (Request) returns (Reply){}
}